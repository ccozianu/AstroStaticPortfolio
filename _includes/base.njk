<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Space+Mono:ital,wght@0,400;0,700;1,400&family=VT323&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="{{ '/style.css' | url }}">
</head>
<body>
    <div class="scanlines"></div>
    <div class="container">
        <header class="bio-header">
            <img src="{{ avatarUrl }}" alt="Profile Avatar" class="avatar">
            <h1>{{ heading }}</h1>
            <p class="tagline">{{ tagline }}</p>
        </header>

        <div class="content-body">
            {{ content | safe }}
        </div>

        <section class="content-section">
            <h2>Connect</h2>
            <ul class="links-list">
                {% for link in socialLinks %}
                <li><a href="{{ link.url }}" class="btn">{{ link.text }}</a></li>
                {% endfor %}
            </ul>
        </section>

        <footer class="footer">
            <p>&copy; 2025 Built with Solarized Love.</p>
        </footer>
    </div>
</body>
</html>
